"""Ownership mapping (CODEOWNERS, meta, git blame)."""

from __future__ import annotations

from pipescope.models import Finding


def analyze() -> list[Finding]:
    """Return ownership / orphan findings (stub)."""
    return []
