"""PipeScope: universal static analyzer for data pipelines.

``__version__`` matches the CLI and JSON ``version`` field. The primary interface
is the ``pipescope`` command-line tool (:mod:`pipescope.cli`).
"""

__version__ = "0.1.2"
